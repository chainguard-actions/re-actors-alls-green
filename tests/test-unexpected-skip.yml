---
name: Test unexpected skipped job causes failure
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          jobs: >-
            {
              "build": {"result": "success", "outputs": {}},
              "required-job": {"result": "skipped", "outputs": {}}
            }
      - name: Assert action step failed (unexpected skip)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action step to fail when a required job was skipped, got '$OUTCOME'"
            exit 1
          fi
          echo "PASS: action correctly failed"
      - name: Assert success output is false
        env:
          SUCCESS: ${{ steps.action.outputs.success }}
        run: |
          echo "success: $SUCCESS"
          if [ "$SUCCESS" != "false" ]; then
            echo "FAIL: expected success=false, got '$SUCCESS'"
            exit 1
          fi
          echo "PASS"
      - name: Assert result output is failure
        env:
          RESULT: ${{ steps.action.outputs.result }}
        run: |
          echo "result: $RESULT"
          if [ "$RESULT" != "failure" ]; then
            echo "FAIL: expected result=failure, got '$RESULT'"
            exit 1
          fi
          echo "PASS"
